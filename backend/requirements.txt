# Open your backend/requirements.txt file and:
# 1. REMOVE this line (if it exists):
#    poppler
#
# Poppler is NOT a Python package - it's installed via apt-get in the CI workflow
#
# 2. Your requirements.txt should look like this (without poppler):

# FastAPI Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# LLM & AI
groq==0.9.0
httpx==0.27.0
langchain==0.3.7
langchain-huggingface
langchain-community==0.3.5
chromadb==0.5.18
sentence-transformers==3.2.1

# Document Processing
pypdf2==3.0.1
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
pillow==11.0.0
pytesseract==0.3.13
pdf2image==1.17.0

# Text-to-Speech
edge-tts==6.1.12

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
numpy==1.26.4


# Install them via: sudo apt-get install poppler-utils tesseract-ocr